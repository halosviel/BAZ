_=[[


TODO:

Fix Pip:
- Add note in boot saying everything is required and called in the same thread
- Validate if it does warn on same name module
- Investigate why wrapping ready() in boot in a thread stops it from running
- Make debugging easier - Start by removing the if statement around game:IsLoaded() in SoundUtils -> boot yields

]]